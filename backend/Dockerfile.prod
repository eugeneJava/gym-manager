ARG DOCKER_HUB_ID
ARG VERSION

FROM eclipse-temurin:22

ARG VERSION

ARG JAR_FILE=backend-${VERSION}.jar
ENV JAR_FILE_IN_CONTAINER=app/${JAR_FILE}

COPY backend/target/${JAR_FILE} ${JAR_FILE_IN_CONTAINER}
CMD java -jar $JAR_FILE_IN_CONTAINER